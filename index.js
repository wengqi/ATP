var config = require('./lib/wrap/gulp-config'),
    doc = require('./lib/wrap/gulp-doc'),
    define = require('./lib/wrap/define');

module.exports = {
    config: config,
    doc: doc,
    define: define
};